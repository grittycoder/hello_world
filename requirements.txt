// Requirements for the project
// This file lists all the dependencies needed to run the project
numpy==1.21.0
pandas==1.3.0
scikit-learn==0.24.2
matplotlib==3.4.2
seaborn==0.11.1
tensorflow==2.5.0
keras==2.4.3
flask==2.0.1
requests==2.25.1
pytest==6.2.4
# Add any additional dependencies below
# For example:
# nltk==3.6.2
# Note: Make sure to use compatible versions of the libraries
# To install the dependencies, run:
# pip install -r requirements.txt

;/* This is a sample requirements.txt file for a Python project. 
   It lists the necessary libraries and their versions to ensure that the project runs smoothly. 
   You can modify this file to include any additional dependencies your project may require. 
   To install the dependencies, use the command: pip install -r requirements.txt */