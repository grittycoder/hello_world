"""
These are shell commands to set up the backend environment; run them in a terminal, not in Python:

cd backend
python3 -m venv venv
source venv/bin/activate  # or venv\Scripts\activate on Windows
pip install -r requirements.txt
"""